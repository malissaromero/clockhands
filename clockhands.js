module.exports = {

  divide: function(val1, val2){
    return val1/val2;

    // if a clock is 360 degrees
    // and the hour is at 12 and minute at 1
    // then 30 degrees should be given back
    // each hour is going to give back 30 degrees

  }
}
